&lt;Window x:Class="CaliburnTreeView.Views.NewWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="300" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="New Window Demo" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" 
                   Text="{Binding Message}" 
                   FontSize="14" 
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="2" 
                   Text="{Binding OpenedTime, StringFormat='Opened at: {0:yyyy-MM-dd HH:mm:ss}'}" 
                   FontSize="12" 
                   HorizontalAlignment="Center"
                   Foreground="Gray"
                   Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="3" 
                   Text="This demonstrates the ability to open new windows from the main shell using Caliburn.Micro 5.0 Beta." 
                   FontSize="12" 
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="0,10"/>
        
        <Button Grid.Row="4" 
                x:Name="CloseWindow"
                Content="Close Window" 
                Width="120" 
                Height="30" 
                HorizontalAlignment="Center"
                Margin="0,20,0,0"/>
    </Grid>
&lt;/Window&gt;