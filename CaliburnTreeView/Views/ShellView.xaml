<Window x:Class="CaliburnTreeView.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CaliburnTreeView.Views"
        mc:Ignorable="d"
        Title="CaliburnTreeView - Caliburn.Micro 5.0 Beta Demo" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="B2VConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Navigation Toolbar -->
        <ToolBar Grid.Row="0" Background="LightGray">
            <Button x:Name="ShowDashboard" Content="Dashboard" Margin="2"/>
            <Button x:Name="ShowFamilyTree" Content="Family Tree" Margin="2"/>
            <Button x:Name="ShowMenuTreeViewModel" Content="Menu Tree" Margin="2"/>
            <Button x:Name="ShowEmployeeTreeViewModel" Content="Employee Tree" Margin="2"/>
            <Separator/>
            <Button x:Name="OpenNewWindow" Content="Open New Window" Margin="2" FontWeight="Bold" Background="LightBlue"/>
        </ToolBar>
        
        <ContentControl x:Name="ActiveItem" Grid.Row="1"/>
    </Grid>
</Window>
